export default {name:'health',aliases:['bot'],ownerOnly:false,requiresAllowedChat:true,requiresAI:false,feature:'system',async execute({sessions,roleManager,reply}){const snapshot={sessions:sessions.snapshot(),roles:roleManager.snapshot()};const connected=snapshot.sessions.filter(s=>s.state==='open').length;await reply(`Night is running. ${connected}/${snapshot.sessions.length} WhatsApp session(s) connected.`);return snapshot;}};
